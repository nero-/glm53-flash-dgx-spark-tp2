bench/.venv/
__pycache__/
*.pyc
.DS_Store

# local reference clones, not part of this repo
ref-tonyd2wild-dflash2/
._*
.DS_Store

# raw bench artifacts — summarize numbers in runs/run-NN-*.md; keep json/txt local
bench/*.json
bench/*.txt
runs/*.json
runs/*.txt
